% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02-model-trajectory_control.R
\name{trajectory_control}
\alias{trajectory_control}
\title{Define trajectory modeling controls}
\usage{
trajectory_control(
  engine = c("spline", "gp"),
  family = "gaussian",
  transform = c("identity", "log1p", "log10p"),
  na_action = c("explicit", "complete"),
  spline_k = 10L,
  spline_basis = c("gam", "natural", "cubic"),
  spline_method = c("REML", "ML", "GCV.Cp"),
  gp_kernel = c("matern32", "rbf", "matern52", "exponential"),
  gp_basis_k = NULL,
  chains = 4L,
  iter = 2000L,
  cores = 1L,
  adapt_delta = 0.95,
  max_treedepth = 12L,
  seed = NULL,
  sparse_min_n = 2L,
  sparse_other_level = "Other",
  missing_level = "Unknown",
  drop_invariant_covariates = TRUE
)
}
\arguments{
\item{engine}{Modeling engine. One of `"spline"` or `"gp"`.}

\item{family}{Response family. Both engines support `"gaussian"`,
`"binomial"`, and `"beta"`. Binomial responses must contain exactly 0 and 1. Beta
responses must lie strictly inside `(0, 1)`; boundary values are rejected
rather than transformed implicitly.}

\item{transform}{Response transformation. One of `"identity"`, `"log1p"`,
or `"log10p"`.}

\item{na_action}{Missing-data handling. `"explicit"` keeps missing
categorical annotations as `missing_level`; `"complete"` removes rows with
missing model variables.}

\item{spline_k}{Basis dimension for spline smooths.}

\item{spline_basis}{Spline basis. `"gam"` uses `mgcv::s()`, `"natural"`
uses `splines::ns()`, and `"cubic"` uses `splines::bs()`.}

\item{spline_method}{Fitting method for `mgcv::gam()`, usually `"REML"` or
`"ML"`.}

\item{gp_kernel}{Gaussian-process covariance kernel. One of `"matern32"`,
`"rbf"`, `"matern52"`, or `"exponential"`.}

\item{gp_basis_k}{Optional number of Hilbert-space basis functions. `NULL`
fits an exact GP; an integer of at least 5 requests the scalable
approximate GP implemented by `brms::gp(k = ...)`.}

\item{chains}{Number of MCMC chains for GP/brms backend.}

\item{iter}{Number of MCMC iterations for GP/brms backend.}

\item{cores}{Number of cores for GP/brms backend.}

\item{adapt_delta}{Target acceptance probability for GP/Stan sampling.}

\item{max_treedepth}{Maximum GP/Stan tree depth.}

\item{seed}{Optional random seed.}

\item{sparse_min_n}{Minimum count for a categorical annotation level. Levels
with fewer samples are collapsed to `sparse_other_level`.}

\item{sparse_other_level}{Label used for collapsed sparse categorical levels.}

\item{missing_level}{Label used for missing categorical annotations when
`na_action = "explicit"`.}

\item{drop_invariant_covariates}{Logical. Whether to remove non-trajectory
covariates that have fewer than two levels after preprocessing.}
}
\value{
A `levaim_trajectory_control` object.
}
\description{
Stores engine, family, transformation, missing-data behavior, and
backend-specific fitting options.
}
