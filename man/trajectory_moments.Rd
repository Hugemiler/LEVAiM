% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08-analysis-trajectory_moments.R
\name{trajectory_moments}
\alias{trajectory_moments}
\title{Summarize fitted trajectories with functional moments}
\usage{
trajectory_moments(
  fit,
  n = 200L,
  time_values = NULL,
  windows = NULL,
  levels = NULL,
  draws = 500L,
  level = 0.95,
  seed = 1L
)
}
\arguments{
\item{fit}{A `levaim_fit`, a list of fits, or `levaim_feature_tests` created
with `keep_fits = TRUE`.}

\item{n}{Number of grid points when `time_values` is not supplied.}

\item{time_values}{Optional numeric time grid.}

\item{windows}{Optional data.frame with `window_label`, `window_start`, and
`window_end`, or `label`, `start`, and `end`.}

\item{levels}{Optional trajectory-group levels to summarize. Other fitted
levels remain part of the model but are omitted from the report.}

\item{draws}{Number of uncertainty draws.}

\item{level}{Interval level.}

\item{seed}{Simulation seed.}
}
\value{
A `levaim_trajectory_moments` data.frame with one row per feature,
  trajectory group, window, and moment.
}
\description{
Produces a table-first summary of fitted trajectory level, timing, variation,
and rates of change over declared time windows. Spline uncertainty is drawn
from the fitted coefficient covariance; GP uncertainty uses posterior fitted
draws.
}
